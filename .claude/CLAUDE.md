# claude-plugins

## Repo Structure
- Marketplace layout: plugins live under `plugins/<name>/`
- `.claude-plugin/marketplace.json` — marketplace index (root)
- Each plugin has its own `.claude-plugin/plugin.json`, hooks, config, tests
